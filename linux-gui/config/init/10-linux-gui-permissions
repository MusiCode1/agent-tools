#!/usr/bin/env bash
set -euo pipefail

install -d -o abc -g abc -m 0700 /config/.cache /config/.cache/ms-playwright
install -d -o abc -g abc -m 0700 /data/browser-profiles /data/browser-projects
install -d -o abc -g abc -m 0750 /data/downloads
